---
title: Instalação
description: Como instalar e configurar o kodda UI no seu projeto
---

## Pré-requisitos

O kodda UI é uma camada acima do [shadcn/ui](https://ui.shadcn.com). Você precisa de:

- **React 18+** e **Next.js 14+** (ou outro framework com suporte a RSC)
- **Tailwind CSS v4** configurado
- **shadcn/ui** já inicializado no projeto (`npx shadcn@latest init`)

## Inicializar

Execute o comando `init` na raiz do seu projeto:

```bash
npx @koddaai/ui init
```

O comando irá:

1. Detectar se shadcn/ui está configurado
2. Validar dependências base (react, tailwindcss)
3. Criar o arquivo `kodda.json` com a configuração
4. Criar a estrutura de diretórios para componentes kodda

### Estrutura criada

```
seu-projeto/
├── components/
│   ├── ui/          # Primitivos shadcn/ui (já existente)
│   └── kodda/      # Compostos kodda UI ← novo
├── hooks/           # Hooks kodda UI ← novo
├── tokens/          # Design tokens ← novo
└── kodda.json      # Configuração ← novo
```

## Adicionar componentes

Instale componentes individualmente:

```bash
npx @koddaai/ui add status-badge
npx @koddaai/ui add data-table
npx @koddaai/ui add page-header
```

O CLI resolve dependências automaticamente. Por exemplo, `data-table` instala:

- Pacotes npm: `@tanstack/react-table`

## Listar componentes

Veja todos os componentes disponíveis:

```bash
npx @koddaai/ui list
```

## Uso

Após instalar, importe diretamente do seu projeto:

```tsx
import { StatusBadge } from "@/components/kodda/status-badge"
import { DataTable } from "@/components/kodda/data-table"
import { PageHeader } from "@/components/kodda/page-header"
```
